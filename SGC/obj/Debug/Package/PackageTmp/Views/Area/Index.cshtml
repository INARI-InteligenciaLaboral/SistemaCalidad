@model IEnumerable<SGC.Models.T_Area>

@{
    ViewBag.Title = "Index";
}

<h2>Áreas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            <label>Nombre Área</label>
        </th>
        <th>
            <label>Clave Área</label>
        </th>
        <th>
            <label>Descripción Área</label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clave_Area)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Area }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Area }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Area })
        </td>
    </tr>
}

</table>
